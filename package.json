{
  "name": "lex-academy",
  "version": "1.0.0",
  "private": true,
  "description": "18-module interactive frontend engineering course delivered as a VS Code extension",
  "scripts": {
    "build": "cd .lesson-runner && node export-prompts.js && cp course-data.json ../extension/media/course-data.json && cd ../extension && pnpm build",
    "build:ext": "cd extension && pnpm build",
    "package": "cd extension && pnpm package"
  }
}
